Pill tab group with a light active fill — the Board / Action Plans / Files switcher.

```jsx
<Tabs tabs={["Board","Action Plans","Files"]} value={tab} onChange={setTab} />
```

For the coral-tinted field selectors on the ticket sheet, use `SegmentedControl` (forms) with `chips` instead.
